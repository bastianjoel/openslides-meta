fields:
  id:
    type: number
    restriction_mode: A
    constant: true
    required: true
  poll_id:
    type: relation
    to: poll/config_id
    required: true
    restriction_mode: A
  allow_abstain:
    type: boolean
    default: true
    restriction_mode: A
  onehundred_percent_base:
    type: string
    required: true
    enum: approval_onehundred_percent_bases
    restriction_mode: A
  required_majority:
    type: string
    enum: required_majority
    default: no_majority
    restriction_mode: A
